{
  "name": "when-all-settled",
  "description": "Like Promise.all, but does not mind if any of the promises reject.",
  "author": "Gerben <gerben@treora.com>",
  "version": "0.1.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/Treora/when-all-settled.git"
  },
  "license": "Unlicense",
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "scripts": {
    "prepare": "babel src -d lib",
    "watch": "babel src -d lib -w",
    "test": "jest --verbose"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.24.1",
    "babel-preset-env": "^1.7.0",
    "jest": "^20.0.4"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
